{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run C++ in integrated terminal",
            "type": "cppdbg",
            "request": "launch",
            "program": "${fileDirname}/${fileBasenameNoExtension}.exe",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${fileDirname}",
            "environment": [],
            "externalConsole": false,   // 🔥 chạy trong terminal tích hợp
            "MIMode": "gdb",
            "miDebuggerPath": "gdb",
            "preLaunchTask": "run"
        }
    ]
}
